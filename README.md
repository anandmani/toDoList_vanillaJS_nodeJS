# toDoList_vanillaJS_nodeJS

Simple todo list using 
  Vanilla JS frontend
  Node.js Express backend
  MYSQL DB
  
DB Schema:
CREATE TABLE `items` (
  `item_id` int(4) NOT NULL,
  `item_name` varchar(100) NOT NULL,
  `item_status` varchar(20) NOT NULL
);
